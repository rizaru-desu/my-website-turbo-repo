{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "~/go/bin/air",
    "build": "go build -o bin/server ./cmd/api/main.go",
    "test": "go test -race -v ./...",
    "lint": "go fmt ./...",
    "check-types": "go vet ./..."
  }
}
